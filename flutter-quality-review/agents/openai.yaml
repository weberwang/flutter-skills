interface:
  display_name: "Flutter Quality Review"
  short_description: "Review Flutter app code and product quality."
  default_prompt: "Use $flutter-quality-review to run F0 project-native checks, Controller F1 triage, read-only F2 lanes, and Controller F3 convergence bound to one candidate SHA."
