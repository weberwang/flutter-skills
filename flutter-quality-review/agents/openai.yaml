interface:
  display_name: "Flutter Quality Review"
  short_description: "Review Flutter app code and product quality."
  default_prompt: "Use $flutter-quality-review to review only the changed risk dimensions after deterministic checks pass, escalating to independent visual or release review when required."
