interface:
  display_name: "Flutter Pen To Architecture"
  short_description: "Optional adapter for explicit Pencil-to-Flutter architecture mapping"
  default_prompt: "Use $flutter-pen-to-architecture only when the user explicitly provides Pencil files or Pencil MCP data. Export selected Pencil nodes through Pencil MCP export_nodes into a Flutter project's assets/images/, verify pubspec.yaml when needed, and fold those assets into Flutter-oriented restoration guidance with tokens, themes, reusable components, and fidelity-versus-Flutterization decisions."
