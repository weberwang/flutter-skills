interface:
  display_name: "Flutter Subagent Delivery"
  short_description: "Coordinate concurrent Flutter writers with isolated worktrees."
  default_prompt: "Use $flutter-subagent-delivery to coordinate disjoint writable Flutter tasks in parallel worktrees, validate candidates, and clean up local task resources."
