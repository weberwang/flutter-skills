interface:
  display_name: "Flutter Subagent Delivery"
  short_description: "Run Flutter design and implementation through subagents."
  default_prompt: "Use $flutter-subagent-delivery for high-risk or concurrent Flutter work with one persistent task worktree, validation before review, targeted re-review, and automatic cleanup."
