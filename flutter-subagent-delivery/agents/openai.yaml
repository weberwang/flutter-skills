interface:
  display_name: "Flutter Subagent Delivery"
  short_description: "Coordinate explicitly authorized parallel Flutter writers."
  default_prompt: "Use $flutter-subagent-delivery only after explicit parallel-writer or worktree authorization, with disjoint scopes and snapshot-bound read-only review."
