source: ../../assets/layout-spec-fidelity-template.yaml
expected: fail
mutations:
  - {op: remove, path: parity_cases.0.target_sha256}
  - {op: remove, path: parity_cases.0.state}
  - {op: remove, path: parity_cases.0.viewport}
