interface:
  display_name: "Flutter HiFi Mockup"
  short_description: "Generate and freeze high-fidelity Flutter mockups."
  default_prompt: "Use $flutter-hifi-mockup with the active visual expression preset and the available image-generation capability to create three distinctive high-fidelity Flutter visual directions at exactly 780 x 1688 px that satisfy the preset direction mix, verify decoded dimensions, let me choose one with a restatable signature, then assess its Flutter implementation path without limiting exploration to stock components or universal restraint."
