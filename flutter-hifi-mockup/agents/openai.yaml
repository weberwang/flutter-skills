interface:
  display_name: "Flutter HiFi Mockup"
  short_description: "Generate module-time page effect images."
  default_prompt: "Use $flutter-hifi-mockup after module effect-image interrogation and Wireframe Review to generate three page-level candidates at exactly 780 x 1688 px, review them against the frozen global direction, and freeze my selected image."
