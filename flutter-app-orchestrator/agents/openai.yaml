interface:
  display_name: "Flutter App Orchestrator"
  short_description: "Coordinate commercial Flutter app delivery."
  default_prompt: "Use $flutter-app-orchestrator to coordinate a commercial Flutter app workflow, using the available image-generation capability for visual direction, an optional review-only prototype for interaction ambiguity, and independent visual QA for user-facing UI review."
