interface:
  display_name: "Flutter App Orchestrator"
  short_description: "Coordinate commercial Flutter app delivery."
  default_prompt: "Use $flutter-app-orchestrator to freeze a global visual-system direction without effect images, then re-run module grilling and use page-level effect images, implementation, audit, and release gates during module delivery."
